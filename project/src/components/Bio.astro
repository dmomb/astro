---
const bio = {
  name: "John Doe",
  title: "Software Architect",
  description: "Passionate software architect with expertise in reactive programming and domain-driven design. Building scalable solutions that make a difference.",
  tags: ["Architecture", "Reactive Programming", "DDD", "Cloud Native", "Full Stack"]
};
---

<section id="bio" class="bio-section">
  <h2 class="text-gradient">Bio</h2>
  <div class="content">
    <h1>{bio.name}</h1>
    <h3>{bio.title}</h3>
    <p>{bio.description}</p>
    <div class="tags">
      {bio.tags.map(tag => <span class="tag">{tag}</span>)}
    </div>
  </div>
</section>

<style>
  .bio-section {
    padding: 2rem;
    border-radius: 8px;
    background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
  }
  .content {
    margin-top: 1rem;
  }
  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 1rem;
  }
  .tag {
    background: rgba(var(--accent-light), 0.1);
    color: rgb(var(--accent-light));
    padding: 0.3rem 0.8rem;
    border-radius: 20px;
    font-size: 0.9rem;
  }
</style>