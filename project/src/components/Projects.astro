---
const projects = [
  {
    title: "Reactive Microservices Platform",
    description: "Built a scalable microservices platform using reactive programming principles and event-driven architecture.",
    tags: ["Reactive", "Microservices", "Event-Driven"],
    link: "#"
  },
  {
    title: "Domain-Driven E-commerce",
    description: "Implemented a complex e-commerce system using DDD patterns and CQRS architecture.",
    tags: ["DDD", "CQRS", "Event Sourcing"],
    link: "#"
  }
];
---

<section id="projects" class="projects-section">
  <h2 class="text-gradient">Projects</h2>
  <div class="projects-grid">
    {projects.map(project => (
      <div class="project-card">
        <h3>{project.title}</h3>
        <p>{project.description}</p>
        <div class="tags">
          {project.tags.map(tag => <span class="tag">{tag}</span>)}
        </div>
        <a href={project.link} class="project-link">Learn More →</a>
      </div>
    ))}
  </div>
</section>

<style>
  .projects-section {
    margin: 2rem 0;
  }
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
  }
  .project-card {
    background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
    padding: 1.5rem;
    border-radius: 8px;
    transition: transform 0.2s;
  }
  .project-card:hover {
    transform: translateY(-5px);
  }
  .project-link {
    display: inline-block;
    margin-top: 1rem;
    color: rgb(var(--accent-light));
    text-decoration: none;
  }
  .tags {
    display: flex;
    gap: 0.5rem;
    margin: 1rem 0;
    flex-wrap: wrap;
  }
  .tag {
    background: rgba(var(--accent-light), 0.1);
    color: rgb(var(--accent-light));
    padding: 0.2rem 0.6rem;
    border-radius: 15px;
    font-size: 0.8rem;
  }
</style>